import { Router } from "express";

const userRoute = Router();

// userRoute.post("/signin", signUpController);
// userRoute.post("/signup", signUpController);
// userRoute.post("/update", signUpController);

export { userRoute };
